{
  "only_ids_wrap_trigger_length": 1000,
  "warning_cumulative_limit": "Response truncated: cumulative response size limit (45,000 bytes) was reached.",
  "warning_line_cap": "Output truncated to 800 lines max. A line counts as one per 2048 characters, so a file of long lines reaches the cap in fewer rows.",
  "error_no_query_match": "No lines matched query: '{}'",
  "warning_header_hierarchy": "Header hierarchy mismatch at line {}: found H{} after H{} without H{}",
  "warning_malformed_link": "Possible malformed link syntax at line {}: unclosed parenthesis or bracket in link",
  "warning_html_syntax": "HTML syntax warning: AST contains error or missing nodes.\n{}",
  "error_query_does_not_compile": "Invalid Tree-sitter query: {}\n{}",
  "error_preview_unknown": "Preview '{}' is unknown, already applied, or expired. Take a fresh one with --dry-run.",
  "error_preview_other_file": "Preview '{}' belongs to {}, not {}. Apply it against the file it was previewed on.",
  "error_preview_stale": "{} changed since preview '{}' was taken, so its diff and syntax check no longer describe the result. Take a fresh one with --dry-run.",
  "error_preview_id_shape": "Invalid preview id '{}'. A preview id looks like 'p1f'.",
  "warning_no_grammar": "No grammar for this file type, so an edit to it is not syntax-checked. Line ids, viewing and editing work as they do anywhere else.",
  "help_footer": "Pass the whole argument object instead with --json '{...}'. `ast-editor skill api` lists every tool's parameters.",
  "error_strict_refused": "Nothing was written. If you judge the parser wrong about this code, apply the batch it kept: ast-editor edit '{}' --apply {}",
  "error_strict_unevaluable": "The syntax check could not run: {}. Nothing was written. The batch is kept: ast-editor edit '{}' --apply {}",
  "message_not_checked": "No grammar covers this file type, so the result was written without a syntax check.",
  "error_target_gone": "No line here is {}. That line is gone — nothing to re-read for it; the ids you hold for other lines are unaffected.",
  "error_address_needs_a_number": "'{}' is a hash with no line number. An address is <number>#<hash>, and the number is what names the line: a hash on its own would land on whatever carries that content now. Read the line again for its id.",
  "error_target_changed": "Line {} changed since you read it. Read that line again for its current id; the ids you hold for other lines are unaffected.",
  "error_target_changed_outside": "Line {} changed outside this tool since you read it. Read that range again; the ids you hold for other lines are unaffected.",
  "error_edit_missing_field": "The '{}' op needs {}, and this one carries {}. `ast-editor skill api` lists what each op takes.",
  "error_pattern_not_found": "The line holds no occurrence {} of '{}'. Read the line again for its current text; nothing was written.",
  "error_file_written_while_read": "{} is being written while it is read, so its line index could not be brought in step with it. Nothing was written. Run this again once the writer has finished.",
  "error_op_takes_one_line": "The '{}' op acts at one line, so it takes a start_id rather than a span, and this one carries an end_id. Nothing was written. Drop end_id, or use replace, delete or move, which span."
}
